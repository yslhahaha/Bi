@{
    ViewBag.Title = "";
    Layout = "~/Areas/Manage/Views/Shared/_Layout.cshtml";
}
<h1>新增目录</h1>
<div class="row col-md-10 col-xs-12">
    <div class="col-md-12">
        @using (Html.BeginForm("Create", "Dir", FormMethod.Post, new { @id = "form1", role = "form", enctype = "multipart/form-data" }))
        {
            @Html.Partial("Partial/_DirPartial")
        }
    </div>
</div>
<script>
    $(function () {
        Submit("btnSave", "form1", "/Manage/Dir/Create", "保存记录成功", true, beforeFun, afterFun);
    });

    var beforeFun = function () {
        //放验证代码,验证不通过返回false
        if (!IsRequired(".IsRequired")) {
            return false;
        }
        return true;
    }

    var afterFun = function (flag) { }
</script>