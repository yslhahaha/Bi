@{
    ViewBag.Title = "";
    Layout = "~/Areas/Manage/Views/Shared/_Layout.cshtml";
}
<h1>新增用户</h1>
<div class="row" style="width:80%;">
    <div class="col-md-12">
        @using (Html.BeginForm("Create", "User", FormMethod.Post, new { @id = "form1", role = "form", enctype = "multipart/form-data" }))
        {
            @Html.Partial("Partial/_UserPartial")
        }
    </div>
</div>
<script>
    $(function () {
        Submit("btnSave", "form1", "/Manage/User/Create", "保存记录成功", true, beforeFun, afterFun);
    });

    var beforeFun = function () {
        //放验证代码,验证不通过返回false
        if (!IsRequired(".IsRequired")) {
            return false;
        }
        return true;
    }

    var afterFun = function (flag) {}
</script>